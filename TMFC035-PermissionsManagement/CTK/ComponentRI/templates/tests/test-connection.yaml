apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "permissionmanagement.fullname" . }}-test-connection"
  labels:
    {{- include "permissionmanagement.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "permissionmanagement.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
