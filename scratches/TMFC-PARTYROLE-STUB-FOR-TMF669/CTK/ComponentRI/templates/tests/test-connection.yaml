apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "partyrolemanagement.fullname" . }}-test-connection"
  labels:
    {{- include "partyrolemanagement.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "partyrolemanagement.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
